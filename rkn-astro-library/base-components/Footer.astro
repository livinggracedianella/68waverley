---
import { fetchPageData } from '../page-components/util.js';
const site = import.meta.env.THIS_SITE
const strapi = import.meta.env.STRAPI_HOST
const navDetails = await fetchPageData(`/api/navigations?filters[key][$eq]=${site}.footer.menu&populate[menu_items][populate][0]=sub_menu_links`);
const menuItems = navDetails.data[0].attributes.menu_items
const logoData = await fetchPageData('/api/navigations?filters[key][$eq]=livinggracedianella.footer.menu&populate[logo][fields][1]=url&populate[logo]fields[1]=alter');
const logoURL = `${strapi}${logoData.data[0].attributes.logo.data[0].attributes.url}`;
const logoAltText = logoData.data[0].attributes.logo.data[0].attributes.alternativeText;
console.log(menuItems);
---
<footer>
</footer>
<style>
    footer {
        display: flex;
        align-items: center;
        flex-direction: column;
        justify-content: center;
        background-color: var(--banner-color);
        min-height: 20vh;
        font-size: 14px;
        padding: 2rem;
    }
</style>